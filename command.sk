#script written by 이루 (youtube.com/@이루05)
#project-horcrux (AKA. 영혼 레이스)

options:
    p: &c&l[!] &f
    NUM_MAX: 999999999

command /t1:
    trigger:
        set {_p1} to location of player
        add 1 to y-coordinate of {_p1}
        set {_p2} to location of getClosesthorcrux(player,player)
        drawParticle({_p1},{_p2})