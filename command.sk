#script written by 이루 (youtube.com/@이루05)
#project-horcrux (AKA. 영혼 레이스)

options:
    p: &c&l[!] &f
    NUM_MAX: 999999999

command /추적 [<player>]:
    trigger:
        set {htarget.%uuid of player%} to arg 1
        send "{@p} &e%{dname.%name of arg 1%}%님을 &c추적 타겟으로 &f지정했습니다"
